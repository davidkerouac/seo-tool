# SEO 工具完整使用指南

## 🚀 快速开始

### 1. 启动服务器

```bash
cd /home/user/seo-tool
npm start
```

服务器会在后台运行在 `http://localhost:3000`

---

## 📝 所有功能使用方法

### 功能 1: 生成 SEO 优化文章

**命令：**
```bash
curl -X POST http://localhost:3000/api/generate-article \
  -H "Content-Type: application/json" \
  -d '{
    "topic": "人工智能的未来发展",
    "keywords": ["AI", "机器学习", "深度学习", "神经网络"],
    "targetWordCount": 2000,
    "sections": 6
  }' | jq .
```

**参数说明：**
- `topic`: 文章主题
- `keywords`: 关键词数组（用于 SEO 优化）
- `targetWordCount`: 目标字数（默认 1500）
- `sections`: 章节数量（默认 5）

**返回示例：**
```json
{
  "success": true,
  "data": {
    "title": "人工智能的未来发展完整指南",
    "html": "<html>...</html>",
    "filepath": "/home/user/seo-tool/output/articles/xxx.html",
    "size": 2882
  }
}
```

**查看生成的文件：**
```bash
ls -lh output/articles/
cat output/articles/xxx.html
```

---

### 功能 2: 生成 Meta 标签

**命令：**
```bash
curl -X POST http://localhost:3000/api/generate-meta-tags \
  -H "Content-Type: application/json" \
  -d '{
    "title": "人工智能完整指南 | AI 学习平台",
    "description": "全面的人工智能学习指南，包含机器学习、深度学习等核心概念，适合初学者和进阶者。",
    "keywords": ["人工智能", "机器学习", "深度学习"],
    "url": "https://example.com/ai-guide",
    "author": "AI 专家",
    "siteName": "AI 学习平台"
  }' | jq .
```

**参数说明：**
- `title`: 网页标题（建议 50-60 字符）
- `description`: 网页描述（建议 120-160 字符）
- `keywords`: 关键词数组
- `url`: 网页完整 URL
- `author`: 作者名称
- `siteName`: 网站名称

**返回的 Meta 标签包含：**
- 基本 meta 标签
- Open Graph（Facebook）标签
- Twitter Card 标签
- SEO 优化标签

---

### 功能 3: 关键词密度分析

**命令：**
```bash
curl -X POST http://localhost:3000/api/analyze-keywords \
  -H "Content-Type: application/json" \
  -d '{
    "text": "人工智能正在改变世界。机器学习是人工智能的核心技术。深度学习让人工智能更加强大。人工智能的应用越来越广泛。",
    "keyword": "人工智能"
  }' | jq .
```

**参数说明：**
- `text`: 要分析的文本内容
- `keyword`: 目标关键词

**返回示例：**
```json
{
  "success": true,
  "data": {
    "density": {
      "keyword": "人工智能",
      "totalWords": 20,
      "keywordCount": 4,
      "density": "20.00",
      "recommendation": "关键词密度过高，可能被视为堆砌。建议降低使用频率。",
      "isOptimal": false
    },
    "topKeywords": [
      {"word": "人工智能", "count": 4, "frequency": "20.00%"},
      {"word": "机器学习", "count": 1, "frequency": "5.00%"}
    ]
  }
}
```

**最佳密度：** 1-3%

---

### 功能 4: SEO 审计

**命令：**
```bash
curl -X POST http://localhost:3000/api/seo-audit \
  -H "Content-Type: application/json" \
  -d '{
    "html": "<html><head><title>人工智能指南</title><meta name=\"description\" content=\"完整的人工智能学习指南，包含机器学习和深度学习的核心概念和实践方法。\"></head><body><h1>人工智能完整指南</h1><p>人工智能正在改变世界...</p><h2>机器学习基础</h2><p>机器学习是 AI 的核心...</p><img src=\"ai.jpg\" alt=\"人工智能\"></body></html>",
    "keyword": "人工智能",
    "name": "my-page-audit"
  }' | jq .
```

**参数说明：**
- `html`: 要审计的 HTML 代码
- `keyword`: 主要关键词
- `name`: 报告文件名

**返回示例：**
```json
{
  "success": true,
  "data": {
    "score": 75,
    "grade": "C",
    "recommendations": [
      "标题长度建议在 50-60 字符",
      "添加更多 H2 标签来组织内容",
      "图片需要添加 alt 属性"
    ],
    "jsonPath": "/home/user/seo-tool/output/reports/my-page-audit.json",
    "htmlPath": "/home/user/seo-tool/output/reports/my-page-audit.html"
  }
}
```

**查看 HTML 报告：**
```bash
cat output/reports/my-page-audit.html
```

HTML 报告包含：
- 总体评分和等级
- Meta 标签分析
- 内容结构分析
- 可读性分析
- 详细改进建议

---

### 功能 5: 生成站点地图 (Sitemap)

**命令：**
```bash
curl -X POST http://localhost:3000/api/generate-sitemap \
  -H "Content-Type: application/json" \
  -d '{
    "urls": [
      {"loc": "/", "changefreq": "daily", "priority": 1.0},
      {"loc": "/blog", "changefreq": "weekly", "priority": 0.9},
      {"loc": "/ai-guide", "changefreq": "weekly", "priority": 0.9},
      {"loc": "/about", "changefreq": "monthly", "priority": 0.7},
      {"loc": "/contact", "changefreq": "monthly", "priority": 0.6}
    ],
    "baseUrl": "https://example.com"
  }' | jq .
```

**参数说明：**
- `urls`: 页面数组
  - `loc`: 页面路径
  - `changefreq`: 更新频率（daily/weekly/monthly）
  - `priority`: 优先级（0.0-1.0）
- `baseUrl`: 网站基础 URL

**返回的 XML 格式站点地图，可直接提交给搜索引擎**

---

## 🎯 实用示例

### 示例 1: 完整的博客文章生成流程

```bash
# 1. 生成文章
curl -X POST http://localhost:3000/api/generate-article \
  -H "Content-Type: application/json" \
  -d '{
    "topic": "Python 编程入门",
    "keywords": ["Python", "编程", "入门教程"],
    "targetWordCount": 1500
  }' | jq '.data.filepath'

# 2. 生成对应的 Meta 标签
curl -X POST http://localhost:3000/api/generate-meta-tags \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Python 编程入门教程 | 编程学习网",
    "description": "完整的 Python 编程入门教程，从零开始学习 Python 编程语言。",
    "keywords": ["Python", "编程", "入门教程"],
    "url": "https://example.com/python-tutorial"
  }' | jq '.data.filepath'

# 3. 检查文章 SEO
# （需要先从上面的命令获取生成的 HTML 内容）
```

---

### 示例 2: 批量生成多篇文章

创建脚本 `batch-generate.sh`:

```bash
#!/bin/bash

topics=(
  "机器学习入门"
  "深度学习基础"
  "神经网络原理"
  "自然语言处理"
)

for topic in "${topics[@]}"; do
  echo "生成文章: $topic"
  curl -s -X POST http://localhost:3000/api/generate-article \
    -H "Content-Type: application/json" \
    -d "{\"topic\":\"$topic\",\"keywords\":[\"AI\",\"机器学习\"],\"targetWordCount\":1500}" \
    | jq '.data.filepath'
  echo ""
done

echo "✅ 所有文章生成完成！"
ls -lh output/articles/
```

运行：
```bash
chmod +x batch-generate.sh
./batch-generate.sh
```

---

## 📂 查看生成的文件

### 查看所有生成的文件
```bash
# 查看文章
ls -lh output/articles/

# 查看 Meta 标签
ls -lh output/meta-tags/

# 查看站点地图
ls -lh output/sitemaps/

# 查看 SEO 报告
ls -lh output/reports/
```

### 查看最新生成的文件
```bash
# 最新的文章
ls -t output/articles/*.html | head -1 | xargs cat

# 最新的 SEO 报告（HTML）
ls -t output/reports/*.html | head -1 | xargs cat

# 最新的 SEO 报告（JSON）
ls -t output/reports/*.json | head -1 | xargs cat | jq .
```

### 统计文件
```bash
echo "文章数: $(ls output/articles/ 2>/dev/null | wc -l)"
echo "报告数: $(ls output/reports/ 2>/dev/null | wc -l)"
echo "总文件数: $(find output -type f 2>/dev/null | wc -l)"
```

---

## 🛠️ 常用组合命令

### 生成文章并立即查看
```bash
filepath=$(curl -s -X POST http://localhost:3000/api/generate-article \
  -H "Content-Type: application/json" \
  -d '{"topic":"AI发展","keywords":["AI"],"targetWordCount":1000}' \
  | jq -r '.data.filepath')

echo "文件已保存到: $filepath"
cat "$filepath"
```

### 分析文本并保存结果
```bash
result=$(curl -s -X POST http://localhost:3000/api/analyze-keywords \
  -H "Content-Type: application/json" \
  -d '{"text":"你的文本内容","keyword":"关键词"}')

echo "$result" | jq . > keyword-analysis.json
echo "结果已保存到 keyword-analysis.json"
```

---

## ⚡ 快速命令备忘

复制这些命令直接使用：

```bash
# 生成文章
curl -X POST http://localhost:3000/api/generate-article -H "Content-Type: application/json" -d '{"topic":"主题","keywords":["关键词"],"targetWordCount":1500}' | jq .

# 分析关键词
curl -X POST http://localhost:3000/api/analyze-keywords -H "Content-Type: application/json" -d '{"text":"文本内容","keyword":"关键词"}' | jq .

# SEO 审计
curl -X POST http://localhost:3000/api/seo-audit -H "Content-Type: application/json" -d '{"html":"<html>...</html>","keyword":"关键词"}' | jq .

# 查看最新文章
ls -t output/articles/*.html | head -1 | xargs cat

# 查看最新报告
ls -t output/reports/*.html | head -1 | xargs cat
```

---

## 💡 提示

1. **所有生成的文件**都保存在 `output/` 目录
2. **文件名包含时间戳**，不会覆盖旧文件
3. **HTML 报告**可以用浏览器打开查看，格式精美
4. **JSON 文件**适合程序处理
5. 使用 `jq` 命令可以格式化 JSON 输出
6. 如果没有 `jq`，可以安装：`apt install jq` 或 `yum install jq`

---

## 🔧 故障排除

### 服务器未启动
```bash
# 检查服务器是否运行
curl http://localhost:3000

# 如果没有响应，启动服务器
npm start
```

### 查看服务器日志
```bash
# 如果服务器在后台运行
tail -f /tmp/claude/-home-user-seo-tool/tasks/*.output
```

### 清理旧文件
```bash
# 清理所有生成的文件（谨慎使用！）
rm -rf output/*

# 或者只清理特定类型
rm -rf output/articles/*
```

---

## 📞 获取帮助

运行示例查看所有功能：
```bash
npm run example
```

运行测试脚本：
```bash
bash test-api.sh
```

交互式使用：
```bash
bash quick-generate.sh
```
